RBT-92's cull_k.py on the throwaway C3 shift arm, seed 801: 4 lines, rule line present: 1
runs/RBT-100/readout.py exit status: 0; 175 lines; stderr lines: 0
sections and gates printed (numbers withheld: throwaway runs, read for nothing):
RBT-100, epoch C3 (scarce food): the readout, from committed tables only, on RBT-92's instrument.
PART 1: RBT-92's readout.py on C3's arms (shift, cull under <scratch>/c3; cull20, onset and base digests under <scratch>/c1)
RBT-92, the first epoch: the readout, from committed tables only.
SEEDS (seed rule: all ten, extinction at or before T - 1 in the baseline the only exclusion)
VALIDATION (on the culls, before the shift is read)
  V0 pre-onset identity: every arm's seasons.txt row equals the baseline's for every season < T, both faunas
  V1 manipulation: events.txt carries exactly the stated cull at T (cull20: min(20, alive) of each fauna; cull:
  V2 round trip: the alive count rebuilt from lineage-last.txt equals seasons.txt's in every season of [T-100, T+200),
  V3 sensitivity: the carriage instrument resolves the k = 20 cull: cull20 - base on L(T+60), holistic, has a
  V3 cull20 - base, L(T+3) holistic: # (n=1, no interval)
  V3 cull20 - base, min paired alive over [...]  below 0 on 1/1
  V3 cull20 - base, min paired alive over [...]  below 0 on 1/1
  V0-V2: PASS on 1/1 seeds;  V3: FAIL
POWER (RBT-89 section 7's line, from each seed's baseline over [T-100, T))
R-BODY: holistic - conventional mean_lifetime_score, window mean, per seed (the co-evolved against designed contrast)
R-SHIFT (shift - base), R-NULL (shift - cull), R-CULL (cull - base), R-CULL20 (cull20 - base): window mean of the per-season difference, per fauna
RECOVERY TIME to the pre-event income plateau (seasons after T; run of 2; 'none' = not within 8); paired form in brackets
ALIVE and deaths: min alive over the transient, recovery, tail; deaths over [T, T+10) (the cull rule's window)
CARRIAGE through the descent DAG (every parent followed; RBT-84's rule) of the onset cohort C0 = alive at T-1  [...]
VERDICT (RBT-89 section 9 on R-body, shift arm, recovery window; r from POWER; sign guard ceil(#n)/n)
  CLASS: NONE: below the six-seed floor
RBT-100 PART 2: the C3-specific lines (PREREGISTRATION.md sections 3, 4, 6); none changes the class
C3-V1 manipulation: the shift arm's events.txt records food.items = 6 from T
  C3-V1: PASS on 1/1
NULL sizing: the impulse k at T against the shift's own demographic cost (deaths only; no income column)
  holistic    : median k 20, median alive deficit at T+30 0 -> the null is not undersized by the pre-registered test
  conventional: median k 20, median alive deficit at T+30 0 -> the null is not undersized by the pre-registered test
ECHO: onsets (seasons after T) of 10-season deaths windows >= 20 in [T, T+10); recovery window starts 3..-3
CLASS D triggers in the shift arm, per seed and fauna (RBT-89 section 9): transient mean < #, recovery mean < #,
DEPTH: median reproduction events after T along the lineages alive at T+7 (first parent followed)

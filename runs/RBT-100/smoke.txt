<scratch>/c3/shift-801/own.txt written; starved + aged (+ culled) = history deaths on 38/38 cells (seasons >= 1)
<scratch>/c3/base-801/own.txt written; starved + aged (+ culled) = history deaths on 38/38 cells (seasons >= 1)
RBT-92's cull_k.py on the throwaway C3 shift arm, seed 801: 6 lines, rule line present: 1
k = 0/0 path on seed 801: C3 cull arm removed
runs/RBT-100/readout.py exit status: 0; 212 lines; stderr lines: 0
readout lines naming the 0/0 null: 1
sections and gates printed (numbers withheld: throwaway runs, read for nothing):
RBT-100, epoch C3 (scarce food): the readout, from committed tables only, on RBT-92's instrument.
PART #: RBT-92's readout.py on C3's arms (shift, cull under <scratch>/c3; cull20, onset and base digests under <scratch>/c1)
RBT-92, the first epoch: the readout, from committed tables only.
SEEDS (seed rule: all ten, extinction at or before T - # in the baseline the only exclusion)
VALIDATION (on the culls, before the shift is read)
  V0 pre-onset identity: every arm's seasons.txt row equals the baseline's for every season < T, both faunas;
  V1 manipulation: events.txt carries exactly the stated cull at T (cull20: min(#, alive) of each fauna; cull:
  V2 round trip: the alive count rebuilt from lineage-last.txt equals seasons.txt's in every season of [T-100, T+#),
  V3 manipulation check on the tracer (adversary F2: guaranteed by construction if the tracer reads its files; it
  V3 cull20 - base, L(T+#) holistic: # (n=#, no interval)
  V3 cull20 - base, min paired alive over [...]  below # on #/#
  V3 cull20 - base, min paired alive over [...]  below # on #/#
  V0-V2: PASS on #/# seeds;  V3: FAIL
POWER (RBT-89 section #'s line, from each seed's baseline over [T-100, T))
R-BODY: holistic - conventional mean_lifetime_score, window mean, per seed (the co-evolved against designed contrast)
R-SHIFT (shift - base), R-NULL (shift - cull), R-CULL (cull - base), R-CULL20 (cull20 - base): window mean of the per-season difference, per fauna
RECOVERY TIME (seasons after T; run of #; 'none' = not within #). PRIMARY, paired against the control (RBT-89 section #; adversary F10): first d with |x_arm - x_base| <= h for # seasons. Secondary, in brackets: against the pre-event plateau P; the base's own P-form d is that form's floor
REMAINDER GROUPS (C1 owes which robots the remainder group held; adversary F5): shift arm, per window and fauna, the
ALIVE and deaths: min alive over the transient, recovery, tail; deaths over [T, T+#) (the cull rule's window)
CARRIAGE through the descent DAG (every parent followed; RBT-84's rule, the protocol's tracer) of the onset cohort
VERDICT (RBT-89 section # on R-body, shift arm, recovery window; r from POWER; sign guard ceil(#n)/n)
  CLASS: NONE: below the six-seed floor
RBT-100 PART #: the C3-specific lines (PREREGISTRATION.md sections #, #, # and Amendment #); none changes the class
C3-V1 manipulation: the shift arm's events.txt records food.items = # from T
  C3-V1: PASS on #/#
NULL, diagnostics only (Amendment #, F1/F2): the impulse null removes k residents at random at T; C3's excess
CLAIM # (Amendment #, rescored; the adversary's wording after its probe P1, n = #, seed #, T = #, not a C3 arm):
  CLAIM #: #/# -> <withheld>
OWN (F3), from own.txt (own_table.py): stored energy a head; the season's own net over the survivors of the season,
  holistic    : own net (all who ran, upper bound) below basal # in the recovery window: shift #/#, base #/# seeds
  conventional: own net (all who ran, upper bound) below basal # in the recovery window: shift #/#, base #/# seeds
ECHO: onsets (seasons after T) of #-season deaths windows >= # in [T, T+#); recovery window starts #..#
CLASS D triggers in the shift arm, per seed and fauna (RBT-89 section #): transient mean < #, recovery mean < #,
DEPTH: median reproduction events after T along the lineages alive at T+# (first parent followed)
FOUNDERS (F5): random founders at six items from season # (founders6-SEED, # seasons, no event): HOLD if >= # alive at
  season #, else FAIL.  The contrast sentence "an established population holds where random founders could not" is
  printed for a seed only where its co-evolved founders FAIL and its established co-evolved fauna <withheld> in the recovery window
  contrast sentence on #/# seeds whose founders fail (#/# founders arms read)
VERDICT TEXT (F6)
    read per seed on founders6: founders HOLD on #/# seeds read; contrast on #/#
  CLASS NONE: below the six-seed floor: as RBT-92's rule prints it.
